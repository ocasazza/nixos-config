scope = "city"
wake_mode = "fresh"
work_dir = ".gc/agents/cc-dreamer"
provider = "claude-code"
nudge = "Survey dreams/memories drift in the active rig, then update."
idle_timeout = "2h"
max_active_sessions = 1
