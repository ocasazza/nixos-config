scope = "city"
wake_mode = "fresh"
work_dir = ".gc/agents/cc-planner"
provider = "claude-code"
nudge = "Read the next ready bead, decompose it into an implementation plan."
idle_timeout = "2h"
max_active_sessions = 1
